import React from 'react'
// import { useAuth } from '../auth/useAuth'

export const LoginPage = ({history}) => {



    return (
        <div>
            LoginPage
            {/* <button
            onClick={handleClick}
            >
                Sign in
            </button> */}
        </div>
    )
}
